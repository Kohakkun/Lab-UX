<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="1dp"
        android:color="#444444" /> <!-- Your desired border color -->
    <corners android:radius="8dp" /> <!-- Optional: Add rounded corners if you like -->
    <solid android:color="@android:color/transparent" /> <!-- IMPORTANT: Make the background transparent so only the border shows -->
</shape>